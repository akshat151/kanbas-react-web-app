const ArrayIndexAndLength = () => {
  let numberArray1 = [1, 2, 3, 4, 5];

  return (
    <div>
      <h2>Array Index and Length</h2>
      <p>length1 = {numberArray1.length}</p>
      <p>index1 = {numberArray1.indexOf(3)}</p>
    </div>
  );
};

export default ArrayIndexAndLength;
